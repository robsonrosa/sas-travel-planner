apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: travel-planner-web-ingress
spec:
  rules: 
    - http:
        paths:
          - pathType: Prefix
            path: /
            backend:
              service:
                name: travel-planner-web-service
                port:
                  number: 80
